-- «Закрепить награду» под фотографией в профиле (правка владельца).
-- Храним только КЛЮЧ ачивки: сами награды, как и XP, выводятся из истории.
-- Валидность ключа проверяет API — награду нельзя закрепить, пока не получена.
ALTER TABLE "users" ADD COLUMN "pinnedAchievement" TEXT;
