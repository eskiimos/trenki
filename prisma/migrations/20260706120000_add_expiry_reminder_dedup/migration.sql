-- Дедуп для уведомления «подписка скоро закончится» (Трек A, п.5).
-- Храним ISO-строку premiumUntil, о которой юзеру уже отправили предупреждение.
-- Value-based (не boolean): при продлении подписки premiumUntil меняется →
-- значение перестаёт совпадать → предупреждение сработает снова за 3 дня до
-- нового конца. Аддитивно, бэкфилл не нужен (NULL = ещё не предупреждали).

ALTER TABLE "users"
  ADD COLUMN "expiryReminderSentFor" TEXT;
