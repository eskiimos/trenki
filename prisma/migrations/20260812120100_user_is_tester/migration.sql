-- Тест-режим (чит-код): булев флаг на пользователе. Даёт тестеру уже
-- существующий «читерский» контур (обход дневного лимита модулей + начисление
-- XP на свободном просмотре), НЕ выдавая при этом прав администратора.
-- Выдаётся/снимается вручную из /admin/users. Безопасный ADD COLUMN с DEFAULT —
-- бэкфилл не нужен.
ALTER TABLE "users" ADD COLUMN "isTester" BOOLEAN NOT NULL DEFAULT false;
