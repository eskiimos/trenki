-- Тестовая касса (правка владельца): платежи через тестовый терминал T-Bank
-- помечаются, чтобы не попадать в «реально платил» — бейдж подписки в админке
-- и счётчик оплаченных интро-периодов считают только боевые.
ALTER TABLE "payments" ADD COLUMN "isTest" BOOLEAN NOT NULL DEFAULT false;
