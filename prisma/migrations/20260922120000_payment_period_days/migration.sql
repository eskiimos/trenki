-- Тариф «3 месяца» (п.12 «Середина сентября»): срок доступа хранится в заказе,
-- выдача и возврат премиума берут его отсюда, а не из константы 30 дней.
-- Все существующие заказы — месячные (DEFAULT 30).
ALTER TABLE "payments" ADD COLUMN "periodDays" INTEGER NOT NULL DEFAULT 30;
