-- Пробный период по промокоду: сколько дней премиума выдать пользователю,
-- зарегистрировавшемуся по этому реф-коду. 0 = без триала.
-- Аддитивно, дефолт 0 — существующие коды поведение не меняют, бэкфилл не нужен.

ALTER TABLE "referral_codes"
  ADD COLUMN "trialDays" INTEGER NOT NULL DEFAULT 0;
