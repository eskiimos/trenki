# Реализация полного алгоритма «Треньки»

## Дата: 24 октября 2025 г.

## ✅ Что реализовано

### 1. Алгоритм определения состояния (п.5) ✅

**Файл:** `src/app/api/training/assessment/route.ts`

**Реализовано:**
- Коэффициент свежести на основе `lastTrainingTime`:
  - TODAY → 1
  - YESTERDAY → 2
  - TWO_DAYS_AGO / THREE_PLUS_DAYS → 3
  - WEEK_PLUS → 1

- Формула: `Уровень_готовности = Коэффициент_свежести + Energy`

- Определение статуса:
  - 2-6 → RECOVERY (восстановление)
  - 7-10 → DEVELOPMENT (развитие)
  - 11-13 → PEAK (пик)

- Автоматический расчет `loadDirection` и `recommendedRPE` на основе статуса

### 2. Таблицы совместимости (п.7-11) ✅

**Файл:** `src/lib/training-compatibility.ts`

**Реализованы функции:**

#### `LOAD_TYPE_MUSCLE_COMPATIBILITY` (п.7)
Совместимость типа нагрузки с направлением нагрузки:
- Сила → все тело, низ, верх жим/тяга, кор стабилизация
- Скорость → низ тела, верх жим, кор динамика
- Выносливость → все группы
- Ловкость → все тело, низ
- И т.д.

#### `FITNESS_TECHNIQUE_COMPATIBILITY` (п.8)
Совместимость техники с физ подготовкой:
- Сила → Техника + мощность
- Скорость → Техника + ловкость
- Выносливость → Техника + скорость/ловкость/мощность
- И т.д.

#### `getWarmupTypes()` (п.9-10)
Подбор типа разминки по статусу и физ подготовке:
- RECOVERY → только динамическая растяжка
- PEAK → только кроссфит (power/speed)
- DEVELOPMENT → зависит от физ подготовки

#### `getWarmupMuscleGroups()` (п.10)
Совместимость направления разминки с физ подготовкой

#### `getCooldownMuscleGroups()` (п.11)
Совместимость заминки с физ подготовкой:
- Все тело → растяжка все тело, ЛФК спина
- Низ → растяжка низ/все тело, ЛФК колено
- И т.д.

#### `getAvailableLoadTypes()` (п.5)
Доступные типы нагрузки по статусу:
- RECOVERY → ЛФК, мобильность, растяжка
- DEVELOPMENT → выносливость, ловкость, скорость
- PEAK → сила, мощность, скорость

### 3. Умный подбор модулей ✅

**Файл:** `src/app/api/training/generate/route.ts`

**Алгоритм работы:**

1. **Определение статуса** из assessment
2. **Подбор FITNESS** (стержневой модуль):
   - Фильтрация по доступным типам нагрузки для статуса
   - Подбор по targetRPE (±2)
   - Исключение недавно выполненных модулей

3. **Подбор WARMUP**:
   - Типы определяются по статусу и типу FITNESS
   - Направление (muscleGroup) совместимо с FITNESS
   - RPE всегда легкий (≤5)

4. **Подбор TECHNIQUE**:
   - Совместимые типы берутся из таблицы FITNESS_TECHNIQUE_COMPATIBILITY
   - Подбор по targetRPE
   - Исключение уже выбранных модулей

5. **Подбор COOLDOWN**:
   - Направление совместимо с FITNESS
   - Типы определяются по статусу
   - RPE всегда легкий (≤4)

### 4. Предотвращение повторов (п.6) ✅

**Реализовано:**
- История выполнения модулей за последние 7 дней
- Исключение недавних модулей при подборе
- Каждый модуль в тренировке уникальный

### 5. Логирование для отладки ✅

Добавлены подробные логи:
```
📊 Assessment calculation
🎯 Selecting modules
✅ Selected FITNESS/WARMUP/TECHNIQUE/COOLDOWN
```

---

## 📋 Что еще можно добавить (не критично)

### 1. Принцип чередования "тяжелых" и "легких" дней
**Статус:** Не реализовано
**Описание:** Автоматическое чередование, если пользователь не заполняет опросы

### 2. Целевая возрастная группа
**Статус:** Структура есть, не используется
**Описание:** Привязка к возрасту пользователя

### 3. Сезонность
**Статус:** Не реализовано
**Описание:** Привязка к календарю хоккейного сезона

### 4. Разные уровни утомления для возрастов
**Статус:** Не реализовано
**Описание:** 
- Дети >7 восстановление; 2-7 развитие; <2 пик
- Подростки >7 восстановление; 4-7 развитие; <4 пик
- Взрослые >6 восстановление; 3-6 развитие; <3 пик

---

## 🧪 Тестирование

### Как проверить:

1. **Создайте assessment:**
```
POST /api/training/assessment
{
  "userId": "dev_123",
  "lastTrainingTime": "WEEK_PLUS",
  "energyLevel": 8,
  "muscleReadiness": 8,
  "motivation": 8,
  "availableTime": 60
}
```

Ожидаемый результат:
- Коэффициент = 1
- Уровень готовности = 1 + 8 = 9
- Статус = DEVELOPMENT
- LoadDirection = MEDIUM

2. **Сгенерируйте тренировку:**
```
POST /api/training/generate
{
  "userId": "dev_123"
}
```

Проверьте в логах:
- Подбор модулей по статусу DEVELOPMENT
- Совместимость модулей между собой
- Правильные типы нагрузки

3. **Проверьте разные сценарии:**
- Energy = 2 → должен быть статус RECOVERY
- Energy = 10 + lastTraining = YESTERDAY (2) = 12 → PEAK
- Energy = 5 + lastTraining = TWO_DAYS_AGO (3) = 8 → DEVELOPMENT

---

## 📊 Сравнение с документом

| Пункт | Описание | Статус |
|-------|----------|--------|
| п.1 | Последовательность из 4 модулей | ✅ Реализовано |
| п.2 | Физ подготовка как стержень | ✅ Реализовано |
| п.3 | Теги модулей | ✅ Реализовано |
| п.4 | Метрика нагрузки (RPE) | ✅ Реализовано |
| п.5 | Учет состояния | ✅ Реализовано |
| п.6 | Предотвращение повторов | ✅ Реализовано |
| п.6 | Чередование | ⏳ Не реализовано |
| п.7 | Совместимость тип/направление | ✅ Реализовано |
| п.8 | Совместимость техника/физ | ✅ Реализовано |
| п.9 | Разминка по статусу | ✅ Реализовано |
| п.10 | Разминка/физ совместимость | ✅ Реализовано |
| п.11 | Заминка/физ совместимость | ✅ Реализовано |

**Реализовано: 11 из 12 пунктов (92%)** ✅

---

## 🚀 Следующие шаги

1. ✅ Заполнить БД модулями тренировок с правильными тегами
2. ✅ Протестировать все сценарии
3. ⏳ Добавить возрастные группы
4. ⏳ Добавить сезонность
5. ⏳ Добавить автоматическое чередование
